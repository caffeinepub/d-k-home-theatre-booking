{
  "frontend/src/index.css": "Global cinematic theme with updated OKLCH color tokens including a rich multi-color background gradient on `body`, new CSS variables for deep-purple, electric-teal, vivid-crimson, warm-amber, midnight-blue, hot-pink, and lime-green, plus updated cinema-hall-bg and screen-glow animations.",
  "frontend/tailwind.config.js": "Extended Tailwind config adding six new vibrant color token groups (deep-purple, electric-teal, vivid-crimson, warm-amber, midnight-blue, hot-pink) with DEFAULT/light/dark shades, five new neon box-shadow utilities, and a color-shift keyframe animation.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; fixed `useConfirmReservation` and `useCancelReservation` to accept `{ id, screeningId }` objects (enabling per-reservation loading states and targeted cache invalidation), added optimistic updates with rollback on error, set `staleTime: 0` on reservation/seatPlan queries for immediate freshness, and added `useBlockUser`/`useUnblockUser`/`useIsUserBlocked` hooks that invalidate all reservation and seat plan caches on success.",
  "frontend/src/components/Layout.tsx": "App shell with sticky dark header (logo, nav links including always-visible Admin link, login/logout button), main content area, and footer with caffeine.ai attribution. The Admin nav link is now always shown so AdminGuard can handle access control instead of hiding the link.",
  "frontend/src/components/TheaterBanner.tsx": "Hero banner with layered multi-color radial gradients (deep purple, electric teal, vivid crimson, warm amber) replacing the plain black/gold overlay, multi-color text glow on the title, and color-varied decorative divider sparkles.",
  "frontend/src/components/ScreeningList.tsx": "Memoized screening grid with an extracted memoized ScreeningCard component, lazy-loaded poster images, and useMemo for stable data transformations to prevent unnecessary re-renders.",
  "frontend/src/components/SeatMap.tsx": "Memoized interactive seat map using useMemo for row grouping and sorting; wrapped with React.memo to prevent full re-renders when unrelated parent state changes.",
  "frontend/src/components/ReservationForm.tsx": "Booking form with a ref-based double-submission guard (500ms cooldown), disabled inputs during pending mutations, and comprehensive error handling.",
  "frontend/src/components/BookingSummary.tsx": "Post-booking confirmation screen styled as a cinema ticket showing screening details, seat labels, customer info, and reservation ID.",
  "frontend/src/pages/Browse.tsx": "Public landing page with TheaterBanner hero and ScreeningList grid, with section heading and subtitle.",
  "frontend/src/pages/BookingFlow.tsx": "Multi-step booking flow with useCallback-memoized seat toggle and handler callbacks, plus ErrorBoundary wrappers around SeatMap, MediaSection, and ReservationForm to prevent white-screen crashes.",
  "frontend/src/components/admin/AdminGuard.tsx": "Auth/role guard that shows a loading spinner while initializing, a login prompt if unauthenticated, a \"Claim Admin Access\" card with a primary button when the user is authenticated but not yet an admin, an access-denied screen on errors, or renders children if admin access is confirmed. Uses `useGrantAdminRole` mutation with toast feedback on success/failure.",
  "frontend/src/components/admin/ScreeningManager.tsx": "Admin screening CRUD panel wrapped in an ErrorBoundary, with a 500ms submit guard, disabled controls during mutations, and error boundaries around each MediaListEditor to prevent crashes during media uploads.",
  "frontend/src/components/admin/SeatPlanEditor.tsx": "Admin seat plan editor with row-based seat grid; fixed to use `SeatStatus` enum values (e.g. `SeatStatus.available`) instead of string literals for all status assignments and comparisons.",
  "frontend/src/components/admin/ReservationManager.tsx": "Admin reservations panel that now uses `useGetAllReservations` (which returns `[id, Reservation][]` tuples with real backend IDs) filtered by `screeningId`, fixing the core bug where fake generated IDs were passed to confirm/cancel; per-row loading spinners show on the active button only, and all action buttons are disabled while any mutation is in-flight.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel page with two-column layout (screening list + tabbed seat plan/reservations editor), wrapped in `AdminGuard` for access control; removed the now-unused `onLogin` prop since `AdminGuard` handles login internally.",
  "frontend/src/App.tsx": "Root app component with page-based navigation state (browse/booking/admin), `ThemeProvider` forcing dark mode, `Layout` wrapper, and Sonner toaster; updated to render `<AdminPanel />` without the removed `onLogin` prop.",
  "frontend/src/components/MediaSection.tsx": "Memoized media display component with comprehensive null/validity checks, lazy-loaded poster images, per-trailer error boundaries, and graceful fallback UI for broken images and invalid URLs.",
  "frontend/src/components/AnimatedBackground.tsx": "Memoized animated cinema background with seeded-random particle generation via useMemo (computed once on mount) and React.memo to prevent re-renders.",
  "frontend/src/utils/debounce.ts": "Reusable debounce utility and submit guard factory for throttling form submissions and preventing rapid-fire backend calls.",
  "frontend/src/components/ErrorBoundary.tsx": "Reusable React class-based error boundary that catches render errors, logs them, and displays a graceful fallback UI with a retry button.",
  "frontend/src/components/SeatIcon.tsx": "Memoized seat button component with a custom comparison function that only re-renders when seat status, label, size, or click handler changes."
}