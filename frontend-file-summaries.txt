{
  "frontend/src/index.css": "Global cinematic dark theme styles with OKLCH color tokens, Playfair Display + Inter fonts, and utility classes for seat states (available/selected/reserved/booked), gold gradients, and theatre-specific design tokens.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Playfair Display display font, Inter sans font, theatre-specific color palette (gold, red, dark, surface, grey), gold box shadows, and shimmer animation.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for the backend — admin check, grant admin role mutation (`useGrantAdminRole` wrapping `actor.grantAdminRole`), user profile, screenings CRUD, seat plan management, and reservations (make/confirm/cancel/list). The new `useGrantAdminRole` hook invalidates `isCallerAdmin` and `callerUserRole` query keys on success.",
  "frontend/src/components/Layout.tsx": "App shell with sticky dark header (logo, nav links including always-visible Admin link, login/logout button), main content area, and footer with caffeine.ai attribution. The Admin nav link is now always shown so AdminGuard can handle access control instead of hiding the link.",
  "frontend/src/components/TheaterBanner.tsx": "Hero banner component displaying the generated theatre image with gradient overlay, D.K Home Theatre branding, and cinematic decorative elements.",
  "frontend/src/components/ScreeningList.tsx": "Grid of screening cards that now displays the first poster image (if available) in a 2:3 aspect ratio above the date strip, with graceful fallback for missing/broken images and a flex-col layout for consistent card heights.",
  "frontend/src/components/SeatMap.tsx": "Interactive public seat map with color-coded seat status buttons; fixed to use `SeatStatus` enum values for all status comparisons instead of string literals.",
  "frontend/src/components/ReservationForm.tsx": "Booking form collecting customer name and contact info; fixed to use `ReservationStatus.pending` enum value instead of the string literal `'pending'`.",
  "frontend/src/components/BookingSummary.tsx": "Post-booking confirmation screen styled as a cinema ticket showing screening details, seat labels, customer info, and reservation ID.",
  "frontend/src/pages/Browse.tsx": "Public landing page with TheaterBanner hero and ScreeningList grid, with section heading and subtitle.",
  "frontend/src/pages/BookingFlow.tsx": "Multi-step booking flow that now renders the MediaSection component (posters + trailers) above the seat map on the seat-selection step, passing the screening's posterImages and trailerLinks arrays.",
  "frontend/src/components/admin/AdminGuard.tsx": "Auth/role guard that shows a loading spinner while initializing, a login prompt if unauthenticated, a \"Claim Admin Access\" card with a primary button when the user is authenticated but not yet an admin, an access-denied screen on errors, or renders children if admin access is confirmed. Uses `useGrantAdminRole` mutation with toast feedback on success/failure.",
  "frontend/src/components/admin/ScreeningManager.tsx": "Admin CRUD panel for screenings with add/edit dialog that now includes poster image URL and trailer video link list editors (add/remove per entry), media indicators on screening cards, and saves posterImages/trailerLinks arrays to the backend.",
  "frontend/src/components/admin/SeatPlanEditor.tsx": "Admin seat plan editor with row-based seat grid; fixed to use `SeatStatus` enum values (e.g. `SeatStatus.available`) instead of string literals for all status assignments and comparisons.",
  "frontend/src/components/admin/ReservationManager.tsx": "Admin reservations list for a given screening with confirm/cancel actions; fixed to pass only the reservation `id` string (not an object) to `confirmReservation.mutateAsync` and `cancelReservation.mutateAsync` to match the updated hook signatures.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel page with two-column layout (screening list + tabbed seat plan/reservations editor), wrapped in `AdminGuard` for access control; removed the now-unused `onLogin` prop since `AdminGuard` handles login internally.",
  "frontend/src/App.tsx": "Root app component with page-based navigation state (browse/booking/admin), `ThemeProvider` forcing dark mode, `Layout` wrapper, and Sonner toaster; updated to render `<AdminPanel />` without the removed `onLogin` prop.",
  "frontend/src/components/MediaSection.tsx": "New component that renders poster images in a scrollable gallery and trailer links as embedded YouTube/Vimeo iframes or \"Watch Trailer\" external link buttons; hidden when no media is attached."
}